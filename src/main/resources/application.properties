spring.application.name=dyMall

# 2025年2月21日
# # MySQL 连接配置
# spring.datasource.url=jdbc:mysql://localhost:3306/dy_mall?serverTimezone=UTC&useSSL=false
# spring.datasource.username=root
# spring.datasource.password=123456
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MySQL 连接配置
spring.datasource.url=jdbc:mysql://localhost:3306/dy_mall?serverTimezone=Asia/Shanghai&useSSL=false
spring.datasource.username=root
spring.datasource.password=123456
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

mybatis.configuration.default-executor-type=simple
spring.datasource.hikari.auto-commit=true


# # JPA 配置（如果使用 Hibernate）
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# # JPA 配置
#  # ✅ 修改成新版本
# # spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# # 避免数据库连接池问题
# spring.jpa.open-in-view=false

# mybatis配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package: com.qxy.dyMall.model
mybatis.configuration.map-underscore-to-camel-case=true

# spring boot自定义配置
# 自定义 Spring Security 的用户名和密码
spring.security.user.name=admin
spring.security.user.password=admin

mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl